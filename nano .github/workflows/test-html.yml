name: Test HTML & CSS

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Validate HTML
      uses: jamesives/github-pages-deploy-action@v4
      with:
        folder: .
